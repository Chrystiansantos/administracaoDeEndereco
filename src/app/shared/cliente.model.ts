export class Cliente {
    constructor(private id: number, private nome: string, private cpf: string, private setor: string, private rua: string,
        private endereco: string, private complemento: string) { }
}